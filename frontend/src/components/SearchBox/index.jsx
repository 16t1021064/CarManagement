import { Grid } from '@mui/material';
import React from 'react';
import styles from './index.module.sass';

function SearchBox() {
  return <Grid xs={3} className={styles.searchbox}>SearchBox</Grid>;
}

export default SearchBox;
